{
  "name": "carousel-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Static, browser-based generator for a four-slide IT career carousel with PNG and ZIP export.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "1.57.0"
  }
}
